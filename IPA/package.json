{
  "name": "ipa",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "apollo-server": "^3.10.1",
    "graphql": "^16.6.0",
    "nexus": "^1.3.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child index.ts",
    "generate": "ts-node --transpile-only schema.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
